import Layout from '../../components/Layout/Layout';
import PromocodesPage from '../../components/Dashboard/DashboardComponents/Promocodes/Promocodes';

const Promocodes = () => {
    return (
        <Layout>
            <PromocodesPage />
        </Layout>
    );
};

export default Promocodes;
